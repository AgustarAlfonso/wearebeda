Build a system that:

ingests the supplied inputs;
classifies each item into a useful business category;
extracts structured information with confidence/uncertainty where appropriate;
identifies duplicates and likely record matches;
determines a recommended next action;
drafts a response where useful;
requires human approval before consequential external action;
creates an understandable audit log of what happened and why; and
provides a simple way to inspect results — CLI, lightweight web UI or structured output is fine.
Please submit:

runnable repository or packaged project;
README with setup instructions;
short screen recording demonstrating it;
short architecture explanation;
AI tools/models used;
known weaknesses and what you would improve with another day.